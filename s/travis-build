#!/bin/sh
set -e
pwd
if [ "" = "`which runtests`" ]; then
echo "installing prerequisites"
set ROOT=$PWD
cd c-tap-harness && ./autogen && ./configure --prefix $ROOT/opt && make install
cd ..
export PATH=$PATH:$ROOT/opt/bin
fi

echo "building project"
make check
